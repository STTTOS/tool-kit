import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.tsx";
import "./main.less";

ReactDOM.createRoot(document.getElementById("compress-image")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
